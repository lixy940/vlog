<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lixy.mapper.custom.OptionsMapperCustom">
	<!-- <resultMap type="HashMap" id="mapOption">
	<result property="key" column="option_name" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result property="value" column="option_value" javaType="java.lang.String" jdbcType="LONGVARCHAR"/>
	</resultMap>  
	<insert id="saveMap" parameterType="java.util.Map">
	  insert into sys_options (option_name, option_value)
    values 
    <foreach collection="map" index="key" item="value" separator=",">
    (#{key,jdbcType=VARCHAR}, #{value,jdbcType=LONGVARCHAR})
    </foreach>
	</insert>
	
	<select id="selectMap" resultType="Options">
	select option_name, option_value from sys_options
	</select> -->

</mapper>